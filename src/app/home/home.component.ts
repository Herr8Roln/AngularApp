import { Component } from '@angular/core';

@Component({
  selector: 'app-home', // Selector for this component
  templateUrl: './home.component.html', // Path to the HTML template
  styleUrls: ['./home.component.css'] // Path to the CSS file
})
export class HomeComponent {
  // Define properties or methods for the component here if needed

}
